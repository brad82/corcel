<?php

return [ 

	/*
	|--------------------------------------------------------------------------
	| Default Database Connection
	|--------------------------------------------------------------------------
	|
	| This option controls the database connection that will be utilised
	| We delegate the registration of connections to the db config.
	|
	| This allows corcel to use a separate connection for wordpress, rather
	| than the default connction. Useful if you need to connect to wordpress
	| via TCP/IP.
	|
	| Supported: Any registered DB connection.
	|
	*/

	'connection' => 'default',
];